<h1> <%= @artist.id ? 'Edit Artist' : 'New Artist' %> </h1>
<%= form_for @artist do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, required: true, autofocus: true %>
  <br/>
  <%= f.submit %>
<% end %>
